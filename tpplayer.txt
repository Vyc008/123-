local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local localPlayer = Players.LocalPlayer

-- Khởi tạo ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "PremiumHitboxMenu"
screenGui.ResetOnSpawn = false
screenGui.Parent = localPlayer:WaitForChild("PlayerGui")

-- NÚT TELEPORT NỔI (Có thể di chuyển độc lập)
local teleportToggleButton = Instance.new("TextButton")
teleportToggleButton.Text = "Dịch chuyển: TẮT"
teleportToggleButton.Size = UDim2.new(0, 140, 0, 40)
teleportToggleButton.Position = UDim2.new(0, 40, 0, 40)
teleportToggleButton.Font = Enum.Font.GothamBold
teleportToggleButton.TextSize = 14
teleportToggleButton.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
teleportToggleButton.TextColor3 = Color3.fromRGB(255, 100, 100)
teleportToggleButton.BorderSizePixel = 0
teleportToggleButton.Active = true
teleportToggleButton.ZIndex = 5
teleportToggleButton.Parent = screenGui

local teleCorner = Instance.new("UICorner")
teleCorner.CornerRadius = UDim.new(0, 8)
teleCorner.Parent = teleportToggleButton


-- KHUNG CHÍNH (Main Panel - Thu gọn chiều cao xuống 225)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 180, 0, 225)
mainFrame.Position = UDim2.new(0, 40, 0, 90)
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 8)
mainCorner.Parent = mainFrame

-- Layout tự động sắp xếp nút trong Main Panel
local mainLayout = Instance.new("UIListLayout")
mainLayout.Parent = mainFrame
mainLayout.SortOrder = Enum.SortOrder.LayoutOrder
mainLayout.Padding = UDim.new(0, 5)
mainLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
mainLayout.VerticalAlignment = Enum.VerticalAlignment.Center

-- Hàm định dạng nhanh cho nút bấm bên trong menu
local function styleButton(btn, text, layoutOrder)
    btn.Text = text
    btn.Size = UDim2.new(0, 160, 0, 30)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 12
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    btn.BorderSizePixel = 0
    btn.TextColor3 = Color3.fromRGB(240, 240, 240)
    btn.LayoutOrder = layoutOrder
    btn.Parent = mainFrame
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn
end

-- Tạo các nút điều khiển trong Menu chính
local toggleListButton = Instance.new("TextButton")
styleButton(toggleListButton, "Chọn người chơi", 1)

local positionButton = Instance.new("TextButton")
styleButton(positionButton, "Vị trí: Phía sau", 2)

-- Khung tăng giảm khoảng cách (Ngang)
local distContainer = Instance.new("Frame")
distContainer.Size = UDim2.new(0, 160, 0, 30)
distContainer.BackgroundTransparency = 1
distContainer.LayoutOrder = 3
distContainer.Parent = mainFrame

local minusButton = Instance.new("TextButton")
minusButton.Text = "-"
minusButton.Size = UDim2.new(0, 30, 1, 0)
minusButton.Font = Enum.Font.GothamBold
minusButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
minusButton.TextColor3 = Color3.fromRGB(240, 240, 240)
minusButton.BorderSizePixel = 0
minusButton.Parent = distContainer
Instance.new("UICorner", minusButton).CornerRadius = UDim.new(0, 6)

local distanceLabel = Instance.new("TextLabel")
distanceLabel.Text = "K.Cách: 4.0"
distanceLabel.Size = UDim2.new(0, 94, 1, 0)
distanceLabel.Position = UDim2.new(0, 33, 0, 0)
distanceLabel.Font = Enum.Font.GothamBold
distanceLabel.TextSize = 11
distanceLabel.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
distanceLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
distanceLabel.BorderSizePixel = 0
distanceLabel.Parent = distContainer
Instance.new("UICorner", distanceLabel).CornerRadius = UDim.new(0, 6)

local plusButton = Instance.new("TextButton")
plusButton.Text = "+"
plusButton.Size = UDim2.new(0, 30, 1, 0)
plusButton.Position = UDim2.new(0, 130, 0, 0)
plusButton.Font = Enum.Font.GothamBold
plusButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
plusButton.TextColor3 = Color3.fromRGB(240, 240, 240)
plusButton.BorderSizePixel = 0
plusButton.Parent = distContainer
Instance.new("UICorner", plusButton).CornerRadius = UDim.new(0, 6)

local toggleFloatButton = Instance.new("TextButton")
styleButton(toggleFloatButton, "Hiện nút nổi: BẬT", 4)
toggleFloatButton.TextColor3 = Color3.fromRGB(100, 255, 100)

-- NÚT Auto Reset (Mặc định hiển thị là ON và màu xanh lá)
local toggleAutoResetButton = Instance.new("TextButton")
styleButton(toggleAutoResetButton, "Tự đặt lại: BẬT", 5)
toggleAutoResetButton.TextColor3 = Color3.fromRGB(100, 255, 100)


-- NÚT "OPEN MENU" LUÔN HIỂN THỊ
local showAllButton = Instance.new("TextButton")
showAllButton.Text = "Mở Menu"
showAllButton.Size = UDim2.new(0, 100, 0, 40)
showAllButton.Position = UDim2.new(0, 190, 0, 40)
showAllButton.Font = Enum.Font.GothamBold
showAllButton.TextSize = 13
showAllButton.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
showAllButton.TextColor3 = Color3.fromRGB(0, 255, 150)
showAllButton.BorderSizePixel = 0
showAllButton.Active = true
showAllButton.Visible = true
showAllButton.Parent = screenGui
Instance.new("UICorner", showAllButton).CornerRadius = UDim.new(0, 8)

-- DANH SÁCH NGƯỜI CHƠI (ScrollingFrame tự bám theo Khung chính)
local frame = Instance.new("ScrollingFrame")
frame.Size = UDim2.new(0, 180, 0, 225)
frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
frame.BorderSizePixel = 0
frame.Visible = false
frame.ScrollBarThickness = 4
frame.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
frame.CanvasSize = UDim2.new(0, 0, 0, 0)
frame.Parent = screenGui
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)

local listLayout = Instance.new("UIListLayout")
listLayout.Parent = frame
listLayout.SortOrder = Enum.SortOrder.Name
listLayout.Padding = UDim.new(0, 5)
listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local listPadding = Instance.new("UIPadding")
listPadding.PaddingTop = UDim.new(0, 8)
listPadding.Parent = frame


-- HÀM DÙNG CHUNG ĐỂ BIẾN GUI THÀNH DRAGGABLE (KÉO THẢ ĐƯỢC)
local function makeElementDraggable(guiObject)
    local dragging, dragInput, dragStart, startPos

    local function update(input)
        local delta = input.Position - dragStart
        guiObject.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end

    guiObject.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = guiObject.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    guiObject.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            update(input)
        end
    end)
end

makeElementDraggable(mainFrame)
makeElementDraggable(teleportToggleButton)
makeElementDraggable(showAllButton)

-- ĐỒNG BỘ VỊ TRÍ DANH SÁCH THEO KHUNG CHÍNH KHI KÉO
RunService.RenderStepped:Connect(function()
    if mainFrame.Visible then
        frame.Position = UDim2.new(mainFrame.Position.X.Scale, mainFrame.Position.X.Offset + 190, mainFrame.Position.Y.Scale, mainFrame.Position.Y.Offset)
    end
end)


-- LOGIC XỬ LÝ TÍNH NĂNG CHÍNH
local selectedPlayer = nil
local teleportEnabled = false
local positionBehind = true
local distanceFromTarget = 4.0
local floatButtonVisible = true
local autoResetEnabled = true -- ĐÃ ĐỔI: Mặc định luôn luôn bật sẵn (true)

-- Hàm làm mới danh sách nút người chơi một cách chính xác nhất
local function updatePlayerListButtons()
    for _, child in pairs(frame:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    
    local totalHeight = 8
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= localPlayer then
            local btn = Instance.new("TextButton")
            btn.Text = player.Name
            btn.Size = UDim2.new(0, 160, 0, 28)
            btn.Font = Enum.Font.Gotham
            btn.TextSize = 12
            btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
            btn.BorderSizePixel = 0
            btn.TextColor3 = Color3.fromRGB(200, 200, 200)
            btn.Parent = frame
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)

            if player == selectedPlayer then
                btn.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
                btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            end

            btn.MouseButton1Click:Connect(function()
                selectedPlayer = player
                updatePlayerListButtons()
            end)

            totalHeight = totalHeight + 33
        end
    end
    frame.CanvasSize = UDim2.new(0, 0, 0, totalHeight)
end

-- Bật/Tắt mở bảng danh sách người chơi
toggleListButton.MouseButton1Click:Connect(function()
    frame.Visible = not frame.Visible
    if frame.Visible then
        updatePlayerListButtons()
    end
end)

-- Cập nhật danh sách và Hủy chọn ngay lập tức khi có người rời phòng
Players.PlayerRemoving:Connect(function(player)
    if selectedPlayer == player then
        selectedPlayer = nil
    end
    if frame.Visible then
        updatePlayerListButtons()
    end
end)

Players.PlayerAdded:Connect(function()
    if frame.Visible then
        updatePlayerListButtons()
    end
end)

-- Logic nút Bật/Tắt Auto Reset bằng tay nếu cần đổi ý
toggleAutoResetButton.MouseButton1Click:Connect(function()
    autoResetEnabled = not autoResetEnabled
    if autoResetEnabled then
        toggleAutoResetButton.Text = "Tự đặt lại: BẬT"
        toggleAutoResetButton.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        toggleAutoResetButton.Text = "Tự đặt lại: TẮT"
        toggleAutoResetButton.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

-- Bật / Tắt Teleport
teleportToggleButton.MouseButton1Click:Connect(function()
    teleportEnabled = not teleportEnabled
    if teleportEnabled then
        teleportToggleButton.Text = "Dịch chuyển: BẬT"
        teleportToggleButton.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        teleportToggleButton.Text = "Dịch chuyển: TẮT"
        teleportToggleButton.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

-- Ẩn / Hiện nút Teleport ngoài màn hình
toggleFloatButton.MouseButton1Click:Connect(function()
    floatButtonVisible = not floatButtonVisible
    teleportToggleButton.Visible = floatButtonVisible
    
    if floatButtonVisible then
        toggleFloatButton.Text = "Hiện nút nổi: BẬT"
        toggleFloatButton.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        toggleFloatButton.Text = "Hiện nút nổi: TẮT"
        toggleFloatButton.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

-- Nhấn nút Open Menu để Đóng / Mở giao diện chính
showAllButton.MouseButton1Click:Connect(function()
    mainFrame.Visible = not mainFrame.Visible
    if not mainFrame.Visible then
        frame.Visible = false
    end
end)

positionButton.MouseButton1Click:Connect(function()
    positionBehind = not positionBehind
    if positionBehind then
        positionButton.Text = "Vị trí: Phía sau"
    else
        positionButton.Text = "Vị trí: Phía trước"
    end
end)

plusButton.MouseButton1Click:Connect(function()
    distanceFromTarget = distanceFromTarget + 0.5
    distanceLabel.Text = "K.Cách: " .. string.format("%.1f", distanceFromTarget)
end)

minusButton.MouseButton1Click:Connect(function()
    distanceFromTarget = math.max(0.5, distanceFromTarget - 0.5)
    distanceLabel.Text = "K.Cách: " .. string.format("%.1f", distanceFromTarget)
end)

local function teleportToPlayer(targetPlayer)
    if not targetPlayer.Character or not targetPlayer.Character:FindFirstChild("HumanoidRootPart") then return end
    local targetHRP = targetPlayer.Character.HumanoidRootPart
    local localCharacter = localPlayer.Character
    if not localCharacter or not localCharacter:FindFirstChild("HumanoidRootPart") then return end
    local localHRP = localCharacter.HumanoidRootPart

    local offset
    if positionBehind then
        offset = -targetHRP.CFrame.LookVector * distanceFromTarget
        localHRP.CFrame = CFrame.new(targetHRP.Position + offset, targetHRP.Position + offset + targetHRP.CFrame.LookVector)
    else
        offset = targetHRP.CFrame.LookVector * distanceFromTarget
        localHRP.CFrame = CFrame.new(targetHRP.Position + offset, targetHRP.Position)
    end
end

-- Vòng lặp kiểm tra trạng thái liên tục
RunService.Heartbeat:Connect(function()
    if selectedPlayer then
        if autoResetEnabled then
            local char = selectedPlayer.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if not char or (hum and hum.Health <= 0) then
                selectedPlayer = nil
                if frame.Visible then
                    updatePlayerListButtons()
                end
            end
        end
    end
    
    if teleportEnabled and selectedPlayer then
        teleportToPlayer(selectedPlayer)
    end
end)
